# Noroff API v2 base URL (defaults to the production API if left unset)
VITE_API_BASE_URL=https://v2.api.noroff.dev

# API key from POST /auth/create-api-key — see the README for how to generate one
VITE_API_KEY=
